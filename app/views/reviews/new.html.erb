<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :thoughts %>
  <%= f.text_area :thoughts %>

  <%= f.label :rating %>
  <%= f.select :rating, Review::VALID_RATINGS %>

  <%= f.submit 'Leave review' %>
<% end %>